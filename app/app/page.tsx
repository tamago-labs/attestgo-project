export default function InboxPage() {
  return (
    <div>
      <h1 className="font-display font-semibold text-2xl">Inbox</h1>
      <p className="mt-2 text-muted text-sm">Your AI composed inbox — salary, invoice and compliant transfer messages.</p>
    </div>
  );
}
